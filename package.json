{
  "name": "parivahan-track",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "packages/*"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace @parivahan/client",
    "dev:server": "npm run dev --workspace @parivahan/server",
    "typecheck": "npm run build --workspace @parivahan/shared && npm run typecheck --workspace @parivahan/client && npm run typecheck --workspace @parivahan/server",
    "build": "npm run build --workspace @parivahan/shared && npm run build --workspace @parivahan/client && npm run build --workspace @parivahan/server"
  }
}
